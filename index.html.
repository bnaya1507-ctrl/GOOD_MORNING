<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>EduFlow | Modern Course Management</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://unpkg.com/react@18/umd/react.development.js"></script>
    <script src="https://unpkg.com/react-dom@18/umd/react-dom.development.js"></script>
    <script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
    <script src="https://unpkg.com/lucide@latest"></script>

    <script>
        tailwind.config = {
            theme: {
                extend: {
                    colors: {
                        primary: '#2563eb',
                        secondary: '#059669',
                        accent: '#7c3aed',
                        neutralBg: '#ffffff',
                        surface: '#f8fafc',
                        textPrimary: '#1e293b',
                        textSecondary: '#64748b',
                        borderClr: '#e2e8f0',
                    },
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                    },
                }
            }
        }
    </script>
    <style>
        body { font-family: 'Inter', sans-serif; background-color: #ffffff; color: #1e293b; }
        .glass-sidebar { border-right: 1px solid #e2e8f0; background: #f8fafc; }
        .custom-shadow { box-shadow: 0 1px 3px 0 rgb(0 0 0 / 0.1), 0 1px 2px -1px rgb(0 0 0 / 0.1); }
    </style>
</head>
<body>
    <div id="root"></div>

    <script type="text/babel">
        const { useState, useEffect } = React;

        // --- Mock Data & State Management ---
        const INITIAL_COURSES = [
            { id: 1, code: 'CS-101', title: 'Introduction to Computer Science', instructor: 'Dr. Smith', students: 45, description: 'Foundational concepts of programming and logic.' },
            { id: 2, code: 'DES-202', title: 'UI/UX Design Systems', instructor: 'Prof. Miller', students: 32, description: 'Building scalable design languages for modern web apps.' },
            { id: 3, code: 'ENG-301', title: 'Advanced Technical Writing', instructor: 'Sarah Jenkins', students: 28, description: 'Mastering documentation and professional communication.' },
        ];

        // --- Components ---

        const SidebarItem = ({ icon, label, active, onClick }) => (
            <div 
                onClick={onClick}
                className={`flex items-center space-x-3 p-3 rounded-lg cursor-pointer transition-all duration-200 ${
                    active ? 'bg-primary text-white shadow-md' : 'text-textSecondary hover:bg-slate-200'
                }`}
            >
                <i data-lucide={icon} className="w-5 h-5"></i>
                <span className="font-medium">{label}</span>
            </div>
        );

        const CourseCard = ({ course }) => (
            <div className="bg-surface border border-borderClr rounded-xl p-6 transition-all hover:shadow-md">
                <div className="flex justify-between items-start mb-4">
                    <span className="bg-blue-100 text-primary text-xs font-bold px-2.5 py-1 rounded">{course.code}</span>
                    <button className="text-textSecondary hover:text-accent transition-colors">
                        <i data-lucide="more-vertical" className="w-5 h-5"></i>
                    </button>
                </div>
                <h3 className="text-lg font-bold text-textPrimary mb-2">{course.title}</h3>
                <p className="text-sm text-textSecondary mb-4 line-clamp-2">{course.description}</p>
                <div className="flex items-center justify-between border-t border-borderClr pt-4">
                    <div className="flex items-center space-x-2">
                        <div className="w-8 h-8 rounded-full bg-accent flex items-center justify-center text-white text-xs">
                            {course.instructor[0]}
                        </div>
                        <span className="text-xs font-medium">{course.instructor}</span>
                    </div>
                    <span className="text-xs text-textSecondary">{course.students} Students</span>
                </div>
            </div>
        );

        // --- Main App Implementation ---
        const App = () => {
            const [user, setUser] = useState({ name: 'Alex Thompson', role: 'Instructor' });
            const [view, setView] = useState('dashboard');
            const [courses, setCourses] = useState(INITIAL_COURSES);

            useEffect(() => {
                lucide.createIcons();
            }, [view]);

            return (
                <div className="flex min-h-screen">
                    <aside className="w-64 glass-sidebar hidden md:flex flex-col p-6 space-y-8">
                        <div className="flex items-center space-x-3 px-2">
                            <div className="w-10 h-10 bg-primary rounded-xl flex items-center justify-center text-white font-bold text-xl">E</div>
                            <span className="text-xl font-bold tracking-tight text-primary">EduFlow</span>
                        </div>
                        
                        <nav className="flex-1 space-y-2">
                            <SidebarItem icon="layout-dashboard" label="Dashboard" active={view === 'dashboard'} onClick={() => setView('dashboard')} />
                            <SidebarItem icon="book-open" label="My Courses" active={view === 'courses'} onClick={() => setView('courses')} />
                            <SidebarItem icon="users" label="Students" active={view === 'students'} onClick={() => setView('students')} />
                            <SidebarItem icon="calendar" label="Schedule" active={view === 'schedule'} onClick={() => setView('schedule')} />
                        </nav>

                        <div className="pt-6 border-t border-borderClr">
                            <SidebarItem icon="settings" label="Settings" active={false} />
                            <SidebarItem icon="log-out" label="Logout" active={false} />
                        </div>
                    </aside>

                    <main className="flex-1 overflow-y-auto bg-white">
                        <header className="h-20 border-b border-borderClr flex items-center justify-between px-8 sticky top-0 bg-white/80 backdrop-blur-md z-10">
                            <div>
                                <h1 className="text-2xl font-bold text-textPrimary">
                                    {view === 'dashboard' ? 'Overview' : 'Course Management'}
                                </h1>
                                <p className="text-sm text-textSecondary">Welcome back, {user.name}</p>
                            </div>
                            
                            <div className="flex items-center space-x-4">
                                <button className="p-2 text-textSecondary hover:bg-surface rounded-full">
                                    <i data-lucide="bell" className="w-5 h-5"></i>
                                </button>
                                <div className="h-10 w-10 rounded-full bg-slate-200 border-2 border-primary overflow-hidden">
                                    <img src={`https://ui-avatars.com/api/?name=${user.name}&background=2563eb&color=fff`} alt="Profile" />
                                </div>
                            </div>
                        </header>

                        {view === 'dashboard' && (
                            <div className="p-8 animate-in fade-in duration-500">
                                <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
                                    <div className="bg-surface p-6 rounded-xl border border-borderClr">
                                        <p className="text-textSecondary text-sm font-medium">Total Students</p>
                                        <h2 className="text-3xl font-bold mt-1">1,284</h2>
                                        <span className="text-secondary text-xs font-bold mt-2 inline-block">↑ 12% this month</span>
                                    </div>
                                    <div className="bg-surface p-6 rounded-xl border border-borderClr">
                                        <p className="text-textSecondary text-sm font-medium">Active Courses</p>
                                        <h2 className="text-3xl font-bold mt-1">14</h2>
                                        <span className="text-primary text-xs font-bold mt-2 inline-block">4 New modules</span>
                                    </div>
                                    <div className="bg-surface p-6 rounded-xl border border-borderClr">
                                        <p className="text-textSecondary text-sm font-medium">Avg. Completion</p>
                                        <h2 className="text-3xl font-bold mt-1">88%</h2>
                                        <div className="w-full bg-slate-200 h-1.5 rounded-full mt-4">
                                            <div className="bg-accent h-1.5 rounded-full w-[88%]"></div>
                                        </div>
                                    </div>
                                </div>

                                <div className="flex justify-between items-center mb-6">
                                    <h2 className="text-xl font-bold">Quick Access</h2>
                                    <button 
                                        onClick={() => setView('courses')}
                                        className="bg-accent text-white px-5 py-2.5 rounded-lg font-semibold hover:opacity-90 transition-all flex items-center"
                                    >
                                        <i data-lucide="plus" className="w-4 h-4 mr-2"></i> Create New Course
                                    </button>
                                </div>

                                <div className="grid grid-cols-1 lg:grid-cols-2 xl:grid-cols-3 gap-6">
                                    {courses.map(course => <CourseCard key={course.id} course={course} />)}
                                </div>
                            </div>
                        )}

                        {view === 'courses' && (
                            <div className="p-8">
                                <div className="bg-surface border border-borderClr rounded-xl overflow-hidden">
                                    <table className="w-full text-left">
                                        <thead className="bg-slate-50 border-b border-borderClr">
                                            <tr>
                                                <th className="px-6 py-4 text-sm font-semibold text-textSecondary uppercase tracking-wider">Course Name</th>
                                                <th className="px-6 py-4 text-sm font-semibold text-textSecondary uppercase tracking-wider">Instructor</th>
                                                <th className="px-6 py-4 text-sm font-semibold text-textSecondary uppercase tracking-wider">Capacity</th>
                                                <th className="px-6 py-4 text-sm font-semibold text-textSecondary uppercase tracking-wider">Action</th>
                                            </tr>
                                        </thead>
                                        <tbody className="divide-y divide-borderClr">
                                            {courses.map((course, idx) => (
                                                <tr key={course.id} className={idx % 2 === 0 ? 'bg-white' : 'bg-slate-50'}>
                                                    <td className="px-6 py-4">
                                                        <div className="font-semibold text-textPrimary">{course.title}</div>
                                                        <div className="text-xs text-textSecondary">{course.code}</div>
                                                    </td>
                                                    <td className="px-6 py-4 text-sm text-textSecondary">{course.instructor}</td>
                                                    <td className="px-6 py-4 text-sm text-textSecondary">{course.students} / 50</td>
                                                    <td className="px-6 py-4">
                                                        <button className="text-primary font-medium hover:underline">Edit</button>
                                                    </td>
                                                </tr>
                                            ))}
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        )}
                    </main>
                </div>
            );
        };

        const root = ReactDOM.createRoot(document.getElementById('root'));
        root.render(<App />);
    </script>
</body>
</html>
